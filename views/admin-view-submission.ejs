<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Institut Mittag-Leffler</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .detail-row {
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid var(--iml-border);
    }
    .detail-row:last-child {
      border-bottom: none;
    }
    .detail-label {
      font-weight: 600;
      color: var(--iml-navy);
      margin-bottom: 0.5rem;
    }
    .detail-value {
      color: var(--text-dark);
      line-height: 1.6;
      white-space: pre-wrap;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="header-content">
      <div class="logo">
        <img src="/images/mittag-leffler-logo.png" alt="Institut Mittag-Leffler Logo">
      </div>
      <div class="header-title">
        <h1>Institut Mittag-Leffler</h1>
        <p>Admin Dashboard</p>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="container">
    <div class="admin-header">
      <h2 class="page-title" style="margin: 0;">Talk Submission #<%= talk.id %></h2>
      <a href="/admin/dashboard" class="btn btn-secondary">← Back to Dashboard</a>
    </div>

    <div class="form-card">
      <div class="detail-row">
        <div class="detail-label">Submitted</div>
        <div class="detail-value"><%= talk.getFormattedDate() %></div>
      </div>

      <div class="detail-row">
        <div class="detail-label">Name</div>
        <div class="detail-value"><%= talk.getFullName() %></div>
      </div>

      <div class="detail-row">
        <div class="detail-label">Email</div>
        <div class="detail-value">
          <a href="mailto:<%= talk.email %>" style="color: var(--iml-gold);"><%= talk.email %></a>
        </div>
      </div>

      <div class="detail-row">
        <div class="detail-label">Affiliation</div>
        <div class="detail-value"><%= talk.affiliation %></div>
      </div>

      <div class="detail-row">
        <div class="detail-label">Talk Title</div>
        <div class="detail-value"><strong><%= talk.talkTitle %></strong></div>
      </div>

      <div class="detail-row">
        <div class="detail-label">Abstract</div>
        <div class="detail-value"><%= talk.talkAbstract %></div>
      </div>

      <% if (talk.questions) { %>
        <div class="detail-row">
          <div class="detail-label">Questions / Additional Comments</div>
          <div class="detail-value"><%= talk.questions %></div>
        </div>
      <% } %>

      <div class="detail-row">
        <div class="detail-label">Email Preferences</div>
        <div class="detail-value">
          <%= talk.sendCopy ? '✓ Requested copy of submission in confirmation email' : '✗ No copy requested' %>
        </div>
      </div>
    </div>

    <div class="btn-group" style="margin-top: 2rem;">
      <a href="/admin/dashboard" class="btn btn-secondary">Back to Dashboard</a>
      <form action="/admin/delete/<%= talk.id %>" method="POST" style="display: inline;"
            onsubmit="return confirm('Are you sure you want to delete this submission?');">
        <button type="submit" class="btn btn-danger">Delete Submission</button>
      </form>
    </div>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <p>&copy; <%= new Date().getFullYear() %> Institut Mittag-Leffler</p>
  </footer>
</body>
</html>
