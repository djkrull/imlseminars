<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Institut Mittag-Leffler</title>
  <link rel="icon" type="image/png" href="/images/mittag-leffler-logo.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    /* Additional styles for Klein Converter */
    .drop-zone {
      border: 2px dashed var(--iml-gold);
      border-radius: 8px;
      padding: 40px;
      text-align: center;
      cursor: pointer;
      transition: all 0.3s ease;
      background: white;
      margin-bottom: 1.5rem;
    }

    .drop-zone:hover,
    .drop-zone.dragover {
      background: var(--iml-cream);
      border-color: var(--iml-dark-gold);
      transform: translateY(-2px);
    }

    .drop-zone.dragover {
      border-style: solid;
    }

    .drop-zone-icon {
      font-size: 48px;
      margin-bottom: 15px;
    }

    .drop-zone-text {
      color: var(--text-dark);
      font-weight: 600;
      margin-bottom: 5px;
    }

    .drop-zone-subtext {
      color: var(--text-muted);
      font-size: 14px;
    }

    .file-input {
      display: none;
    }

    .selected-file {
      background: #d4edda;
      border: 1px solid #c3e6cb;
      border-radius: 8px;
      padding: 12px 15px;
      margin-bottom: 1.5rem;
      display: none;
      align-items: center;
      gap: 10px;
    }

    .selected-file.visible {
      display: flex;
    }

    .selected-file-icon {
      font-size: 24px;
    }

    .selected-file-info {
      flex: 1;
    }

    .selected-file-name {
      color: var(--success-green);
      font-weight: 600;
      font-size: 14px;
    }

    .selected-file-size {
      color: #558b2f;
      font-size: 12px;
    }

    .status {
      margin-top: 1.5rem;
      padding: 1rem;
      border-radius: 8px;
      display: none;
      font-size: 14px;
      line-height: 1.6;
    }

    .status.visible {
      display: block;
    }

    .status.loading {
      background: #e3f2fd;
      color: #1565c0;
      border-left: 4px solid #1565c0;
    }

    .status.success {
      background: #d4edda;
      color: var(--success-green);
      border-left: 4px solid var(--success-green);
    }

    .status.error {
      background: #f8d7da;
      color: var(--error-red);
      border-left: 4px solid var(--error-red);
    }

    .spinner {
      display: inline-block;
      width: 16px;
      height: 16px;
      border: 2px solid var(--iml-gold);
      border-top: 2px solid transparent;
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
      margin-right: 8px;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    .info-box {
      background-color: var(--iml-light-gray);
      border-left: 4px solid var(--iml-gold);
      padding: 1rem 1.25rem;
      margin-top: 1.5rem;
      border-radius: 4px;
      font-size: 14px;
      color: var(--text-muted);
      line-height: 1.5;
    }

    .info-box strong {
      color: var(--iml-navy);
    }

    .step-list {
      list-style: none;
      padding: 0;
      margin: 8px 0 0 0;
    }

    .step-list li {
      padding: 4px 0;
    }

    .step-list li:before {
      content: "✓ ";
      color: var(--iml-gold);
      font-weight: bold;
      margin-right: 6px;
    }

    .button-group {
      display: flex;
      gap: 1rem;
    }

    .button-group .btn {
      flex: 1;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="header-content">
      <img src="/images/IML_bla.png" alt="Institut Mittag-Leffler - The Royal Swedish Academy of Sciences" style="height: 80px; width: auto;">
    </div>
  </header>

  <!-- Main Content -->
  <main class="container">
    <h2 class="page-title">Klein Schedule Converter</h2>
    <p class="page-subtitle">Convert Kleindagarna schedule to Ventla import format</p>

    <div class="form-card">
      <div class="drop-zone" id="dropZone">
        <div class="drop-zone-icon">📁</div>
        <div class="drop-zone-text">Drop your Kleindagarna Excel file here</div>
        <div class="drop-zone-subtext">or click to browse (max 10MB)</div>
        <input type="file" id="fileInput" class="file-input" accept=".xlsx,.xls" />
      </div>

      <div class="selected-file" id="selectedFile">
        <div class="selected-file-icon">✅</div>
        <div class="selected-file-info">
          <div class="selected-file-name" id="fileName"></div>
          <div class="selected-file-size" id="fileSize"></div>
        </div>
      </div>

      <div class="button-group">
        <button class="btn btn-primary" id="convertBtn" disabled>Convert to Ventla Format</button>
        <button class="btn btn-secondary" id="clearBtn" disabled>Clear</button>
      </div>

      <div class="status" id="status"></div>

      <div class="info-box">
        <strong>How it works:</strong>
        <ul class="step-list">
          <li>Upload your Kleindagarna schedule Excel file</li>
          <li>Click "Convert to Ventla Format"</li>
          <li>Download the converted file (ProgramImport.xlsx)</li>
          <li>Import the file into Ventla</li>
        </ul>
        <p style="margin-top: 1rem;"><strong>Note:</strong> The schedule should have dates in "Tisdag 13 januari" format and times in "HH:MM-HH:MM" format.</p>
      </div>
    </div>

    <!-- Back to Home Link -->
    <div style="text-align: center; margin-top: 2rem;">
      <a href="/" class="btn btn-secondary">← Back to Registration</a>
    </div>
  </main>

  <!-- Footer -->
  <footer style="background-color: var(--iml-light-gray); padding: 2rem; text-align: center; margin-top: 4rem; border-top: 1px solid var(--iml-border);">
    <p style="color: var(--text-muted); margin-bottom: 0.5rem;">&copy; 2025 Institut Mittag-Leffler</p>
    <p style="color: var(--text-muted);">The Royal Swedish Academy of Sciences</p>
  </footer>

  <script>
    const dropZone = document.getElementById('dropZone');
    const fileInput = document.getElementById('fileInput');
    const selectedFile = document.getElementById('selectedFile');
    const convertBtn = document.getElementById('convertBtn');
    const clearBtn = document.getElementById('clearBtn');
    const status = document.getElementById('status');
    const fileName = document.getElementById('fileName');
    const fileSize = document.getElementById('fileSize');

    let selectedFileData = null;

    // Drag and drop handlers
    dropZone.addEventListener('click', () => fileInput.click());

    dropZone.addEventListener('dragover', (e) => {
      e.preventDefault();
      dropZone.classList.add('dragover');
    });

    dropZone.addEventListener('dragleave', () => {
      dropZone.classList.remove('dragover');
    });

    dropZone.addEventListener('drop', (e) => {
      e.preventDefault();
      dropZone.classList.remove('dragover');
      const files = e.dataTransfer.files;
      if (files.length > 0) {
        handleFile(files[0]);
      }
    });

    fileInput.addEventListener('change', (e) => {
      if (e.target.files.length > 0) {
        handleFile(e.target.files[0]);
      }
    });

    function handleFile(file) {
      // Validate file type
      if (!file.name.endsWith('.xlsx') && !file.name.endsWith('.xls')) {
        showError('❌ Invalid file type. Please upload an Excel file (.xlsx or .xls)');
        return;
      }

      // Validate file size (max 10MB)
      if (file.size > 10 * 1024 * 1024) {
        showError('❌ File is too large. Maximum size is 10MB');
        return;
      }

      selectedFileData = file;

      // Show selected file
      fileName.textContent = file.name;
      fileSize.textContent = `${(file.size / 1024).toFixed(1)} KB`;
      selectedFile.classList.add('visible');

      // Enable buttons
      convertBtn.disabled = false;
      clearBtn.disabled = false;

      // Clear status
      status.classList.remove('visible');
    }

    clearBtn.addEventListener('click', () => {
      selectedFileData = null;
      fileInput.value = '';
      selectedFile.classList.remove('visible');
      convertBtn.disabled = true;
      clearBtn.disabled = true;
      status.classList.remove('visible');
    });

    convertBtn.addEventListener('click', async () => {
      if (!selectedFileData) return;

      const formData = new FormData();
      formData.append('file', selectedFileData);

      convertBtn.disabled = true;
      clearBtn.disabled = true;

      showLoading('Converting your schedule to Ventla format...');

      try {
        const response = await fetch('/api/klein-converter/convert', {
          method: 'POST',
          body: formData,
          credentials: 'same-origin'
        });

        if (response.ok) {
          // Download the file
          const blob = await response.blob();
          const url = window.URL.createObjectURL(blob);
          const a = document.createElement('a');
          a.href = url;
          a.download = 'ProgramImport.xlsx';
          document.body.appendChild(a);
          a.click();
          window.URL.revokeObjectURL(url);
          document.body.removeChild(a);

          showSuccess('✅ Successfully converted! Your file has been downloaded.<br/>You can now import it into Ventla.');

          // Auto-clear after 3 seconds
          setTimeout(() => {
            clearBtn.click();
          }, 3000);
        } else {
          const data = await response.json();
          showError(data.error || 'An error occurred during conversion');
          convertBtn.disabled = false;
          clearBtn.disabled = false;
        }
      } catch (error) {
        showError(`Connection error: ${error.message}`);
        convertBtn.disabled = false;
        clearBtn.disabled = false;
      }
    });

    function showLoading(message) {
      status.innerHTML = `
        <div>
          <span class="spinner"></span>
          <span>${message}</span>
        </div>
      `;
      status.className = 'status loading visible';
    }

    function showSuccess(message) {
      status.innerHTML = message;
      status.className = 'status success visible';
    }

    function showError(message) {
      status.innerHTML = `❌ ${message}`;
      status.className = 'status error visible';
    }
  </script>
</body>
</html>
